# Deep experiment pipeline — keep all 5 GPUs saturated
#
# Format: <name> <steps> [ENV_VAR=value ...]

# === Wave 19: threshold position ===
# relu zeros at x=0. Is 0 special, or does shifting help/hurt?
# Narrower dead zone than relu (zero in [-0.2, 0.2] vs relu's (-inf, 0])
# Hard threshold at 0.5 (discontinuous). Tests if the kink matters.
# Symmetric sparsity: shrink from both sides. Tests if negative suppression is directional.

# === Wave 20: elu family (same positive side as relu, different negative sides) ===
# elu² is running in act18. These add more data points along the negative-side axis.
# softplus centered at zero: smooth, passes through origin, linear for large x.
# Replicate relu2 baseline as a control for this wave.
# abs2 but only on positive side (= relu2, sanity check that act path works)
act20_leaky05_500 500 MLP_ACT=leaky_relu2_05

# === Wave 21: baseline calibration + depth check ===
# We need relu2 and abs2 on multiple seeds to know if the gap is real.
act21_relu2_seed42_500 500 MLP_ACT=relu2 SEED=42
act21_relu2_seed99_500 500 MLP_ACT=relu2 SEED=99
act21_abs2_seed42_500 500 MLP_ACT=abs2 SEED=42
act21_abs2_seed99_500 500 MLP_ACT=abs2 SEED=99
# Extend abs2 to 1000 steps — does it hold or reverse like AttnRes did?
act21_abs2_1000 1000 MLP_ACT=abs2
