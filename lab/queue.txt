# Deep experiment pipeline — keep all 5 GPUs saturated
#
# Format: <name> <steps> [ENV_VAR=value ...]

# === Wave 19: threshold position ===
# relu zeros at x=0. Is 0 special, or does shifting help/hurt?
# Narrower dead zone than relu (zero in [-0.2, 0.2] vs relu's (-inf, 0])
# Hard threshold at 0.5 (discontinuous). Tests if the kink matters.
# Symmetric sparsity: shrink from both sides. Tests if negative suppression is directional.

# === Wave 20: elu family (same positive side as relu, different negative sides) ===
# elu² is running in act18. These add more data points along the negative-side axis.
# softplus centered at zero: smooth, passes through origin, linear for large x.
# Replicate relu2 baseline as a control for this wave.
# abs2 but only on positive side (= relu2, sanity check that act path works)

# === Wave 21: baseline calibration + depth check === [DONE]
# abs² confirmed across 3 seeds: mean 1.4698 vs relu² mean 1.4805 (Δ = 0.0107)
# abs2_1000 still pending (empty dir)

# === Wave 22: 2000-step depth check for top activations === [DONE]
# relu² worst of 5 at every checkpoint. leaky(0.5)²=1.3218 best, relu²=1.3264 worst.
# Rankings at 500 steps = rankings at 2000 steps. 500-step ablations are reliable.

# === Wave 23: WHY does squaring work? ===
# Three hypotheses:
#   H1 (magnitude): squaring just makes outputs bigger → test relu_scaled (2·relu, no squaring)
#   H2 (adaptive gradient): grad ∝ activation is per-neuron adaptive LR
#       → test relu_detach2/abs_detach2 (same output as ²,  but gradient halved)
#   H3 (feature interaction): x² creates quadratic features → wins by elimination if H1+H2 fail
# Also: map optimal leak rate sweep (0.3, 0.7 to fill gaps in 0/0.01/0.1/0.5/1.0)
