# Tier 3: test positive-side linearity hypothesis
#
# Hypothesis: relu² wins because relu's positive side is pure linear (f(x) = x),
# giving the cleanest x² after squaring. Other functions curve near zero.
#
# Format: <name> <steps> [ENV_VAR=value ...]

# sharp_softplus approaches relu as beta grows. Does performance approach relu² too?

# elu has the SAME positive side as relu (f(x)=x for x>0) but smooth negative side.
# If this matches relu², the positive side is what matters, not the hard cutoff.

# |x|² = x² everywhere. Linear on both sides, no zeros. Tests raw linearity.

# Zero in [-0.5, 0.5], identity outside. More sparsity than relu.
act18_hard_shrink2_500 500 MLP_ACT=hard_shrink2
